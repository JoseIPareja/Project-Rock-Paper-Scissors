function computerPlay() {
    let options = ["Rock", "Paper", "Scissors"];
    return options[Math.floor(Math.random() * options.length)];
}

function playRund(playerSelection, computerSelection) {
    //return a string that declares the wi 
}